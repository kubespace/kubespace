ARG BASEIMAGE=registry.cn-hangzhou.aliyuncs.com/kubespace/distroless-static:latest
FROM $BASEIMAGE

COPY bin/amd64/kubespace /
COPY apps /apps
COPY ui/dist/favicon.ico /favicon.ico

CMD ["/kubespace"]

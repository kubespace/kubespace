apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubespace.fullname" . }}-test-connection"
  labels:
    {{- include "kubespace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubespace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
